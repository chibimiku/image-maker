你将基于最终的英文图片描述，重新计算 pixiv_tags，确保标签准确反映图片中实际出现、可见的视觉内容。

CRITICAL 内容要求：pixiv_tags 中的每个标签必须描述图片中**实际出现、可见**的视觉内容（如人物特征、服装、动作、表情、道具等），而不是泛泛的「pixiv 风格」标签，也不要输出与画面无关的标签。每个标签都应能在描述中找到对应的明确依据。

标签选择规则：
1. pixiv_tags 中每个标签必须是日语（日文），禁止使用英文或中文标签。标签格式如：「女の子」「青い空」等。
2. 标签总数必须严格为 12 个。
3. 优先覆盖描述中明确提到的关键视觉元素，按以下优先级选取：
   - 人物核心特征：人数、发色、瞳色等（最高优先级，必须保留）。
   - 服装主体与标志性配饰：如外套、裙子、帽子、领带等与人物形象直接绑定的元素。
   - 服装细节与材质：如蕾丝、刺绣、过膝袜、吊带等。
   - 动作姿态与表情：如踮脚、片足立ち、微笑等。
   - 标志性道具：如ポッキー 等在画面中明确出现的道具。
4. 不要输出纯环境/背景类标签（如「雨夜」「小巷」「ネオン」等），聚焦人物与服装。
5. 如果描述中未明确提及某元素，不要凭空添加该元素的标签。
6. 当需要在核心特征标签与其他标签之间取舍时，始终保留核心特征标签。
7. 不要输出表示面部质量退化或审查的标签（如 mosaic、blurry、censored 等）。

输入数据（最终描述与参考标签）：
{input_json}

预期输出 JSON 结构：
{{
    "pixiv_tags": ["<日文标签1>", "<日文标签2>", "<日文标签3>", "<日文标签4>", "<日文标签5>", "<日文标签6>", "<日文标签7>", "<日文标签8>", "<日文标签9>", "<日文标签10>", "<日文标签11>", "<日文标签12>"],
    "reason": "<简要说明标签选择依据，尤其是保留了哪些核心特征标签>"
}}
